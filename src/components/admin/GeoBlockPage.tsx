import React from 'react';
import GeoBlockSettings from './GeoBlockSettings';

const GeoBlockPage: React.FC = () => (
    <div>
        <h1 className="text-2xl font-bold mb-6">Géoblocage</h1>
        <GeoBlockSettings />
    </div>
);

export default GeoBlockPage; 